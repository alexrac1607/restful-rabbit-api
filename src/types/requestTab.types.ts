export enum ResponseDisplayScreen {
  Response = "Response",
  Headers = "Headers",
}

export enum ResponseDataScreen {
  Params = "Params",
  Body = "Body",
  Headers = "Headers",
  Authorization = "Authorization",
}
